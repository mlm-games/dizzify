<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude launcher settings/state (contains settings-lock PIN hash + home layout).
         Users can export/import explicitly via Settings instead. -->
    <exclude domain="file" path="datastore/launcher_settings.preferences_pb" />
    <exclude domain="file" path="datastore/launcher_state.preferences_pb" />
    <exclude domain="file" path="datastore/" />
    <exclude domain="database" path="datastore/" />
</full-backup-content>
